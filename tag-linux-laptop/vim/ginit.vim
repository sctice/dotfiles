" Required because `set guifont=` doesn't work with recent versions of Iosevka in Neovim-Qt, and
" the GuiFont! command (which bypasses warnings) is only recognized from this initialization file.
GuiFont! Iosevka:h11

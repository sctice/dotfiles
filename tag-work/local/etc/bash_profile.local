# See the common bash_profile for an explanation of the differences between
# bash_profile and bashrc, and what should go where.

# Print some basic server statistics (remove whitespace from the beginning of
# the output of uptime).
echo; uptime | sed -e 's/^ *//'

# Print out a summary of CR leaders over the past few weeks
echo -e "\nCR Leaders\n"; cr-leaders | head -n7; echo

# vim: sw=2 ts=2 sts=2 ft=sh

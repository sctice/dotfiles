setlocal spell

" I'd like to use setlocal here, but pencil doesn't respect it (it ends up setting the local
" textwidth from the global option). I like using pencil for git commits, and I'm almost always
" editing git commits in a dedicated vim instance anyhow.
set textwidth=72
